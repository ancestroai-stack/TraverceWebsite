# Cloudflare Pages — URL Rewrites
# Serves files at clean URLs (no .html extension needed)
# 200 = rewrite (keeps the clean URL in the browser bar)

/backstage    /backstage.html    200
/admin        /admin.html        200
