{
  "name": "travweb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node sync_artists.js",
    "dev": "vite",
    "prebuild": "node sync_artists.js",
    "build": "vite build",
    "dist": "npm run build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "vite": "^8.0.9"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "html-to-image": "^1.11.13"
  }
}
